# UNO Server 实施规划

> 对应：`docs/功能需求.md`、`docs/技术方案.md`  
> 原则：小步可运行、可手测；规则引擎纯内存；规则与网络解耦；每请求/每 WS 包结束一条带 `trace_id` 的日志。

**状态：** MVP 路线完成
**最后更新：** 2026-08-08

---

## 1. 当前进度

| 阶段 | 状态 | 说明 |
|------|------|------|
| 文档 / 目录 / `go.mod` | ✅ 完成 | 需求、技术方案、骨架目录 |
| **M0-a HTTP 登录 + 日志** | ✅ 完成 | 游客登录、healthz、logx、访问日志 |
| **M0-b WebSocket 会话** | ✅ 完成 | 鉴权、hello、ping/pong、入站路由 |
| M1 房间 | ✅ 完成 | create/join/ready/start/mailbox |
| M2 规则引擎 | ✅ 完成 | `game/uno`、快照、安全视图与单测 |
| M3 房间挂牌局 | ✅ 完成 | 玩法协议、私有广播、完整一局 |
| M4 超时/托管/重连（内存） | ✅ 完成 | 回合计时、自动托管、断线占座、Session 换绑 |
| M5 MySQL 三表 | ✅ 完成 | players、matches、match_results、事务结算 |
| **M6 Redis 快照 + 重启恢复** | ✅ 完成 | token、原子房间快照、启动恢复、停机刷快照 |
| **M7 打磨** | ✅ 完成 | 空房 GC、Prometheus 指标、日志字段、README 与 Compose |

### 已落地代码（M0-a～M7）

| 路径 | 职责 |
|------|------|
| `cmd/server` | 进程入口、信号退出 |
| `internal/config` | 环境变量配置，含回合托管、空房回收、指标、MySQL 与 Redis 参数 |
| `internal/idgen` | 41/10/12 位雪花业务主键生成器 |
| `internal/auth` | 游客登录、players 写入、token 签发、内存热缓存与 Redis 回源校验 |
| `internal/logx` | Info/Warn/Error + `WithContext` + `trace_id` |
| `internal/protocol` | WS 外壳、房间与牌局消息契约 |
| `internal/session` | WS 双泵、出站队列、业务路由与断线回调 |
| `internal/room` | RoomManager、mailbox、Engine、托管重连、空房回收与持久化边界 |
| `internal/game/uno` | 108 张牌、玩法状态机、叠罚、UNO、结算、快照与安全视图 |
| `internal/store/mysql` | database/sql 连接池、幂等建表和三表 Repository |
| `internal/store/redis` | go-redis 连接池、token TTL、Lua 原子房间快照与启动扫描 |
| `internal/observability` | 独立 Prometheus Registry、低基数业务指标与运行时 Gauge |
| `internal/server` | HTTP/WS 接入及房间、完整一局、持久化与重启恢复集成测试 |
| `Dockerfile` / `compose.yaml` | 非 root 应用镜像、健康检查、MySQL/Redis 本地编排 |

### 已确认约定

| 项 | 约定 |
|----|------|
| 通道 | 登录 HTTP；房间/对局 WS（含创建/加入） |
| 日志 | 一 HTTP 请求 / 一 WS 包结束只打一条；`logger.WithContext(ctx).Info/Warn/Error` |
| 错误 | 标准库 `errors` + `%w` |
| token 默认 TTL | 24h（`UNO_TOKEN_TTL`） |
| 人数 | 2–6 |
| 规则要点 | 叠罚可叠、摸后可出、无 Wild+4 同色限制、做 UNO 质疑 |

---

## 2. 总体路线图

```
M0-a ✅ 登录/日志
   │
   ▼
M0-b ✅ WS 会话（鉴权 + hello + ping/pong）
   │
   ▼
M1   ✅ 房间（create/join/ready/start 门槛 + 广播）
   │
   ├──────────────┐
   ▼              ▼
M2  ✅ 规则引擎、快照与安全视图
   │
   ▼
M3  ✅ 房间挂 Engine，完整一局
   │
   ▼
M4  ✅ 超时托管 + 断线重连（内存）
   │
   ▼
M5  ✅ MySQL：players / matches / match_results
   │
   ▼
M6  ✅ Redis：token + 房间快照 + 启动恢复
   │
   ▼
M7  ✅ 打磨（空房 GC、观测、README 与本地编排）
```

---

## 3. 本次完成：M6 Redis 快照与重启恢复

### 3.1 目标

让随机 token 和进行中牌局跨进程存活：服务重启后，客户端继续使用原 token 完成 WS 鉴权，RoomManager 从 Redis 重建成员、完整私有手牌、牌堆、行动阶段、MySQL `match_id` 和回合截止时间，并沿用 M4 的 Session 换绑流程继续游戏。

内存仍是运行期唯一可写真相；Redis 是崩溃恢复副本。单个快照写失败只记录错误，不回滚已经生效的玩法动作；配置了 Redis 但启动连接或全量扫描失败时拒绝监听，避免把可恢复房间静默当成空状态。

### 3.2 分层与依赖方向

```text
auth ──────→ store.SessionRepository ───┐
                                        ├─→ store/redis ─→ go-redis 连接池
room ──────→ store.RoomSnapshotRepository┘

room ──────→ uno.Engine.Snapshot / uno.Restore
game/uno ──→ 仍为无 IO 的纯规则引擎
```

Redis 仓储只理解 token 会话值和不透明房间 JSON；房间快照结构、版本校验和 Engine 恢复均留在 `internal/room` 与 `internal/game/uno`。进程入口只创建一个并发安全的 go-redis 客户端，让会话与快照共享连接池。

### 3.3 Redis 键与原子操作

| 键 | 值 | 生命周期 |
|----|----|----------|
| `uno:session:{token}` | `player_id`、昵称、绝对过期时间 JSON | 与 token 剩余 TTL 一致 |
| `uno:player_room:{player_id}` | `room_id` | 随活跃房间续期，离房时条件删除 |
| `uno:room:{room_id}` | 完整房间与 Engine JSON | 每个状态事件后续期 |
| `uno:room_index` | 活跃 `room_id` 集合 | 启动批量扫描；自动清理悬空成员 |

房间保存通过 Lua 在单 Redis 实例内原子执行 `SET PX`、`SADD` 与全部玩家索引更新。删除快照时会先比较 `player_room` 当前值，迟到的旧房清理不会误删玩家后来加入的新房间。本期部署边界仍为单进程、单 Redis 节点；以后切 Redis Cluster 时需统一哈希标签后再保留跨键脚本。

### 3.4 快照内容与写入边界

房间 JSON 包含 `schema_version`、单调 `revision`、`updated_at`、房间阶段、房主、稳定座位、连接/托管/连续超时状态、`turn_deadline`、`match_id` 和 `match_started_at`。进行中房间还嵌入 `uno.Snapshot`，覆盖 108 张实体牌归属、牌堆、弃牌堆、当前座位与颜色、罚牌池、摸牌决策、待选色动作、UNO 抓罚窗口及胜者候选。

| 触发 | Redis 行为 | 业务失败语义 |
|------|------------|--------------|
| 登录 | MySQL players 成功后写 token，再发布内存热缓存 | Redis 失败则登录失败，不返回半成品 token |
| 建房/加入/准备/开局 | mailbox 事件结束后同步写完整快照 | 房间动作保留，错误日志记录恢复副本落后 |
| 出牌/摸牌/选色/UNO/自动托管 | 每个成功或可能已改状态的命令后同步覆盖 | 不让 Redis 错误回滚权威 Engine |
| 断线/重连/离房 | 刷新成员状态；离房条件删除玩家索引 | 内存路由立即生效 |
| 终局/空房 | 删除房间、集合与仍指向本房的玩家索引 | MySQL 结算仍按 M5 事务边界处理 |
| 优雅退出 | 停止 HTTP 接入、关闭 WS，再向每间 mailbox 投递最终刷快照与停止命令 | 受统一 shutdown context 限制 |

### 3.5 启动恢复

1. 启动时先连接 MySQL 与 Redis，并执行 `PING`。
2. 通过 `uno:room_index` + `MGET` 按房间号稳定加载全部 JSON，同时清除缺少值的集合成员。
3. 校验房间 schema、修订号、成员唯一性、房主、阶段、座位顺序和 MySQL 对局元数据。
4. 调用 `uno.Restore` 再次校验阶段不变量、108 张牌守恒、待处理动作和终局结果。
5. 重建 Room 与 mailbox；全部 Session 指针清空、`connected=false`，进行中成员统一 `auto_play=true`。
6. 复用快照中尚未到期的回合截止时间；已到期时至少等待一次托管间隔。
7. 重建内存 `player→room` 路由并立即写回新的断线状态，随后才开始 HTTP 监听。
8. 单个损坏、版本不兼容、已经终局或玩家身份冲突的快照会被隔离并清理，不阻断其它合法房间恢复。

### 3.6 配置

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `UNO_REDIS_ADDR` | 空 | 为空时保持内存模式，非空时启用 Redis |
| `UNO_REDIS_USERNAME` / `UNO_REDIS_PASSWORD` | 空 | ACL 用户名与密码 |
| `UNO_REDIS_DB` | `0` | 单节点逻辑数据库编号 |
| `UNO_REDIS_POOL_SIZE` | `10` | 最大连接池大小 |
| `UNO_REDIS_MIN_IDLE_CONNS` | `1` | 最小空闲连接数 |
| `UNO_REDIS_DIAL_TIMEOUT` | `3s` | 建连超时 |
| `UNO_REDIS_READ_TIMEOUT` / `UNO_REDIS_WRITE_TIMEOUT` | `2s` | 命令读写超时 |
| `UNO_REDIS_OPERATION_TIMEOUT` | `2s` | 登录、快照、恢复的业务超时 |
| `UNO_REDIS_ROOM_SNAPSHOT_TTL` | `2h` | 活跃房间与玩家索引兜底 TTL |

### 3.7 验收结果

- [x] token 写入 Redis 后才发布内存缓存；新 Service 实例可用原 token 回源鉴权
- [x] token TTL 到期自动失效，过期热缓存会同步清理
- [x] Lua 保存/删除覆盖快照、room_index、player_room 和迟到清理保护
- [x] waiting 与 playing 房间均可恢复；进行中 Engine 快照逐字段保持一致
- [x] 损坏快照被隔离清理，Redis 全量加载失败会阻止启动
- [x] 真实 WebSocket 集成测试完成“原 token 重连、恢复原私有手牌、继续行动”
- [x] Redis 使用内存服务器测试，无需依赖本机 Docker 服务
- [x] 新增函数与复杂边界使用中文注释

### 3.8 后续边界

M6 不做多实例房间抢占、Redis Cluster 跨槽脚本、快照防抖和 MySQL 终局补偿队列；这些边界在 M7 仍保持不变。

---

## 4. 本次完成：M7 稳定性与交付打磨

### 4.1 空房回收

房间以“没有任何同时满足 `connected=true` 且持有 Session 的成员”作为空连接状态。首次变为空连接时启动 `UNO_EMPTY_ROOM_TTL` 计时，默认保留 15 分钟给玩家重连；等待中、进行中和已结算房间使用相同的资源上限。

计时器只投递带版本号的 `commandEmptyRoomTimeout`，不直接修改 Room。重连会停止计时并递增版本，mailbox 消费超时事件时再次检查版本与连接状态，因而迟到事件不会销毁已经重连的房间。确认回收后按顺序执行：

1. 在与 `submit` 相同的互斥边界发布 closing 状态，拒绝新命令。
2. 停止回合与空房计时器，标记房间销毁。
3. 从 Manager 的 `rooms` 和 `playerRoom` 索引移除。
4. 在 mailbox 退出前删除 Redis 房间快照和仍指向本房的玩家索引。
5. 唤醒已经排在回收事件之后的同步命令，避免调用方永久等待。

恢复出的房间成员统一断线，因此也会立即开始空房保留计时。进行中房间在保留期内仍可托管推进；若始终无人回来，最终由 GC 给进程内存与 Redis 状态提供确定上界。

### 4.2 Prometheus 指标

`UNO_METRICS_ENABLED=true` 时注册 `/metrics`。每个 Server 使用独立 Registry，不触碰 Prometheus 全局注册表，测试和同进程多 Server 场景不会发生重复注册。除 Go 运行时和进程指标外，提供：

| 指标 | 类型 | 固定维度 |
|------|------|----------|
| `uno_server_http_requests_total` | Counter | method、固定 route、status_class |
| `uno_server_http_request_duration_seconds` | Histogram | method、固定 route |
| `uno_server_websocket_connections` | GaugeFunc | 无 |
| `uno_server_websocket_messages_total` | Counter | result |
| `uno_server_room_active` | GaugeFunc | 无 |
| `uno_server_room_garbage_collections_total` | Counter | phase |
| `uno_server_room_restores_total` | Counter | result |

客户端可控的任意 HTTP 方法会折叠为 `OTHER`，未匹配路径折叠为 `unmatched`，WS 仅记录固定结果分类。`room_id`、`player_id`、连接 ID 和错误文本不进入标签，避免高基数污染。

### 4.3 结构化日志

进程、HTTP、WebSocket、Redis 快照、恢复、结算和 GC 日志统一增加稳定 `event` 字段；状态码统一为 `status_code`，连接、远端地址和响应字节分别使用 `connection_id`、`remote_addr`、`response_bytes`。错误边界附加固定 `result` 与 `error`，HTTP/WS 请求仍遵守“一次边界操作一条完成日志”。

### 4.4 本地运行与交付

- README 覆盖 Docker 与本机启动、HTTP/WS 协议、完整环境变量、恢复语义、指标、测试和上线检查。
- Dockerfile 使用多阶段静态构建、非 root 运行用户及 `/healthz` 健康检查。
- Compose 一次启动应用、MySQL 8.4 与 Redis 7.4，依赖健康后才启动应用，并使用命名卷保存数据。
- `.env.example` 只提供本地开发占位值，`.env` 被 Git 与 Docker 构建上下文排除。

### 4.5 验收结果

- [x] 恢复出的无人房间会在可配置保留期后清理内存路由与 Redis 快照
- [x] 重连使旧 GC 事件失效，回收退出不会遗留永远等待的同步命令
- [x] 指标端点可关闭；开启后覆盖 HTTP、WS、房间、恢复与 GC 核心信号
- [x] 指标标签均为固定低基数，运行时 Gauge 直接读取 Manager 当前状态
- [x] 关键日志字段统一，并保留 `trace_id` 请求关联
- [x] Docker Compose 与 README 可作为完整本地启动入口
- [x] 新增函数和复杂并发边界使用中文注释

### 4.6 后续边界

M0–M7 的单实例 MVP 路线已经完成。后续若继续演进，优先考虑多实例房间所有权租约、Redis Cluster 键槽设计、MySQL 结算补偿队列、快照写入防抖与压测基线；这些属于下一轮架构目标，不混入本次提交。

---

## 5. 阶段摘要

### M1 房间（已完成）

| 交付 | 要点 |
|------|------|
| RoomManager | roomId → Room |
| Room mailbox | 单协程串行 |
| 消息 | create_room / join_room / leave / ready / start（仅校验人数与准备，可暂不真正开牌） |
| 广播 | 成员列表、准备状态 |
| 日志 | 每条房间相关 WS 消息一条 |

**验收：** 2～3 客户端同房状态一致。

### M2 规则引擎（已完成）

| 交付 | 要点 |
|------|------|
| `internal/game/uno` | 牌堆、合法性、叠罚、Reverse/Skip、结算 |
| 单测 | 表驱动覆盖主路径与边界 |
| 无 IO | 不依赖 session/room/store |

**验收：** `go test ./internal/game/uno/...` 绿。

### M3 联调一局（已完成）

Room 开局创建 Engine；出牌/摸牌/选色/UNO；私有手牌 vs 公开桌面；非法操作拒绝。  
**验收：** 2 人手打完一局。

### M4 超时与重连（内存，已完成）

回合定时器事件进 mailbox；断线占座 + 托管；重连全量快照。  
**验收：** 拔线再连能续打。

### M5 MySQL（已完成）

`players` / `matches` / `match_results`；登录写 players；开局/结算写库。  
**验收：** 局后库中有记录。

### M6 Redis 重启（已完成）

token 会话；Lua 原子房间快照；启动加载；全员断线恢复；优雅退出刷盘。
**验收：** 新服务实例使用原 token 恢复原手牌并继续出牌。

### M7 打磨（已完成）

空房回收、指标可选、日志字段统一、README 与本地依赖编排。
**验收：** 无人房间有确定资源上界，核心运行信号可抓取，并能用一条 Compose 命令启动完整环境。

---

## 6. 编码与质量约定（全程）

| 项 | 要求 |
|----|------|
| 注释 | 新增与修改注释统一使用中文；函数级与复杂并发边界优先覆盖 |
| 日志 | 仅边界打点（HTTP 中间件 / WS 消息结束）；`WithContext` |
| 错误 | `errors.Is` / `fmt.Errorf("%w")` |
| 依赖方向 | `session/room → game`；`game` 不依赖网络与 DB |
| 单文件 | 尽量 < 500 行，函数 < 50 行 |
| 测试 | 新公共能力尽量有单测；规则包覆盖优先 |

---

## 7. 风险与顺序纪律

| 风险 | 规避 |
|------|------|
| 过早上房间+规则 | 先 M0-b 管道再 M1 再 M2 |
| 出牌写 DB | 禁止；仅结算写 MySQL |
| 在 Room 协程里直接 Write WS | 只投递 session.send |
| 无 trace 刷屏日志 | 中间路径不打访问日志 |

---

## 8. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-08-08 | M7 完成；加入 mailbox 空房回收、Prometheus 指标、统一日志字段、README 和 Docker Compose；M0–M7 MVP 路线完成 |
| 2026-08-08 | M6 完成；加入 Redis token、Lua 原子房间快照、启动恢复与原 token WebSocket 续打；下一步调整为 M7 |
| 2026-08-08 | M5 完成；加入雪花 ID、MySQL 连接池、players 登录写入及 matches/results 事务结算；下一步调整为 M6 |
| 2026-08-08 | M0-b～M4 完成；加入回合超时、自动托管、断线占座和同 token 重连；下一步调整为 M5 |
| 2026-08-04 | 初稿；标记 M0-a 完成，详细拆分 M0-b |
